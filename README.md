
  # Convert to React.js site

  This is a code bundle for Convert to React.js site. The original project is available at https://www.figma.com/design/VFkDK1cG6xHwxqPobUsuJf/Convert-to-React.js-site.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  